<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.caster.notes.dsl">

  <application
      android:allowBackup="true"
      android:name=".NotesApplication"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme"
      tools:ignore="AllowBackup">
    <activity
        android:name="com.caster.notes.dsl.features.list.presentation.NotesListActivity"
        android:label="@string/app_name"
        android:windowSoftInputMode="adjustPan"
        android:theme="@style/AppTheme.NoActionBar">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".features.details.presentation.NoteDetailsActivity"
        android:label="@string/add_note"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/AppTheme.NoActionBar"/>
    <activity android:name=".common.showcase.NotesListDSLActivity"
        android:label="@string/app_name"
        android:windowSoftInputMode="adjustPan"
        android:exported="true"
        android:theme="@style/AppTheme.NoActionBar"/>
    <activity android:name=".common.showcase.NotesListActivity"
        android:label="Notes"
        android:windowSoftInputMode="adjustPan"
        android:exported="true"
        android:theme="@style/AppTheme.NoActionBar"/>
  </application>

</manifest>